Following Android tutorials from https://www.youtube.com/user/slidenerd

Created a (non working) calculator using LinearLayouts to learn how to nest layouts and break down a display design into smaller pieces. 

However, the nested layouts in the number pad caused the buttons to not quite line up correctly. This could probably be fixed by using custom margin padding for those containers and buttons, instead of using the same margins for all buttons.